<div style="background-color: {{ entry.color }}">
	<div class="p-6 md:p-14 flex items-start flex-col justify-between">
		<div>
			<div class="prose text-white">
				{{ entry.discipline }}
			</div>
			<h3 class="text-2xl font-medium leading-tight">
				{{ entry.title }}
			</h3>
			<div class="prose mt-4 text-white">
				{{ entry.intro }}
			</div>
		</div>
		<div class="my-14">
			<img class="" src="https://source.unsplash.com/random/440x490" alt="" />
		</div>
		<a class="bg-primary items-start w-auto py-2 px-6 hover:bg-primaryHover rounded-full"
			href="{{ entry.externalLink }}"
			target="_blank">
			View Site
		</a>
	</div>

	{# {{ entry.title }}
	{{ entry.externalLink }}
	{{ entry.intro }}
	<a href="{{ first.externalLink }}">View Site</a> #}
</div>
