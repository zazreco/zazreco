<div class="px-6 mx-auto max-w-7xl sm:px-6 lg:px-8 w-full {{ outerClass|default(null) }}">
    <div class="mx-auto max-w-6xl w-full {{ innerClass|default(null) }}">
        {% block children %}

        {% endblock %}
    </div>
</div>